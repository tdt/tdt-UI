{{ include('header.twig') }}

    <div class='col-lg-12'>
        <h1>Input Management</h1>
    </div>

    <form action="" role="form" method="post">
        <div class='col-lg-6'>
            <div class="form-group">
                <label>Type</label>
                <select class="form-control" name='type'>
                    {% for key, option in types  %}
                        <option value='{{key}}'>{{option.text}}</option>
                    {% endfor %}
                </select>
                <span class='help-block'>Source type of the file that you want to map into triples</p>
            </div>
            <div class="form-group">
                <label>Data file</label>
                <input type="text" class="form-control" placeholder="URL to data file" name='inputfile'>
                <span class='help-block'>The raw data that will be extracted and mapped into triples</p>
            </div>
            <div class="form-group">
                <label>Mapping file</label>
                <input type="text" class="form-control" placeholder="URL to mapping file" name='mappingfile'>
                <span class='help-block'>A mapping file should map the data onto ontological concepts</p>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-default" name='testmapping' >Test mapping</button>
                <button type="submit" class="btn btn-primary" name='addjob' value='addjob'>Add job</button>
            </div>
        </div>
    </form>


    <div class='col-lg-6'>
        <div class="panel">
            <div class="panel-heading">
                Guide
            </div>
            <div class="panel-body">
                <p>
                    Test your mapping file which maps your data into triples before publishing it. Hit the "Test mapping" button in order to see the mapping in action.
                </p>
                <p>
                    When the result is correct, you can add it as a job by clicking the "Add job" button. This will publish your soon to be semantic data.
                </p>
            </div>
        </div>
    </div>

{{ include('footer.twig') }}